import React from 'react'

export const ContentManagement = () => {
  return (
    // <main>

    //   {Lesson.map((lesson, index) => (
    //     <article key={lesson.id}>
    //       <h2>{`LESSON ${index + 1}`}</h2>
    //       <div>
    //         <h3>{lesson.type}</h3>
    //         <span>{lesson.level}</span>
    //       </div>
    //       <div>
    //         <h4>{lesson.title}</h4>
    //       </div>

    //       <button onClick={() => handleToggleLesson(lesson.id)} disabled={lesson.published}>
    //         {lesson.published ? "Published" : " Unpublished"}
    //       </button>

    //       <button onClick={() => handleDeleteLeson(lesson.id)} type="button">DELETE</button>
    //     </article>
    //   ))}

    // </main>
  )
}
